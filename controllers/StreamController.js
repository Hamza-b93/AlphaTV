const mongoose = require("mongoose");
const schema = streamModelSchema = require('../models/StreamSchema.js');

const getStream = async function (req, res) {
  console.log("Route Working!");
};

module.exports = {
  getStream
};
